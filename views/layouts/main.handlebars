<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NiftyGifty</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
  </head>

  <body>
    <div class="height">
      <header>

        <nav>
          <div class="navbar">
            {{#if logged_in}}
              <a class="" href="/dashboard">Dashboard</a>
              <a class="" href="/">Home</a>
              <button class="" onclick="logout()" id="logout">Logout</button>
            {{else}}
              <a class="" href="/login">Dashboard</a>
              <a class="" href="/">Home</a>
              <a class="" href="/login">Login</a>
            {{/if}}
          </div>
          <h1 class="display-2 ml-1 d-flex justify-content-center">Technology
            Forum</h1>
        </nav>
      </header>
      <main class="container">
        {{{body}}}
      </main>
    </div>

  </body>

  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if}}
</html>